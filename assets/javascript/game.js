// The player will be shown a random number at the start of the game. 
// A computer will generate a random number between 19-120 - look at activity 
// Store random number in a container
var randomNumber = Math.floor((Math.random() * 120) + 19);

// Linking ID from HTML to display computer number
document.getElementById('computer').innerHTML = randomNumber;

// Keep track of player's wins and losses
// Store values in container

//Display images on the page making each image have specific value generated by computer between 1-12
// look at activity 
// when user clicks on each image, the images random value gets added to the user score
if user score is = randomnumber, then user wins 
if user score is greater than randomnumber, then user loses

//Have user score display in container

